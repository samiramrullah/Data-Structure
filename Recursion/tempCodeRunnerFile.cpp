if(arr.size()==0|| arr[arr.size()-1]<=temp)
    // {

    //     arr.push_back(temp)
    // }
    // int val=arr[arr.size()-1];
    // arr.pop_back();
    // insert(arr,temp);
    // arr.push_back(val);